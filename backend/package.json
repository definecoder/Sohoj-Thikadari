{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build":"npx prisma generate && npx prisma migrate deploy",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "npx prisma migrate dev --name init --create-only": "npx prisma migrate dev --name init --create-only --schema=./database/Schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "http-status-codes": "^2.3.0",
    "jsonwebtoken": "^9.0.2",
    "mailgen": "^2.0.28",
    "mysql2": "^3.6.1",
    "nodemailer": "^6.9.7",
    "prisma": "^5.5.2",
    "sequelize": "^6.33.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
