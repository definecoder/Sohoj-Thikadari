import { useLocation } from "react-router-dom";

export default function HomePage() {

    // getting uid from login or signup :v
    //console.log(useLocation().state?.uid);

    return <>
        <h1>hello</h1>
    </>;

}